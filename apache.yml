---
- name: install apache server on centos 7
  hosts: webserver
  become: yes
  remote_user: semaphore
  become_method: sudo
  tasks: 
  - name: httpd package install
    yum:
     name: httpd
     state: latest
